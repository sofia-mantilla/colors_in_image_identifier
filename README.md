# colors_in_image_identifier
A simple, powerful Flask web application that detects dominant colors in any uploaded image using K-Means clustering. It displays both the original image and a pie chart of color proportions, and lets the user exclude specific colors on the result page to instantly recalculate percentages.
